
global:
  redis:
    password: bhXvm2p7Xj
  kibanaEnabled: false

vergon:
  affinityNotMaster: &affinityNotMaster
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: kubernetes.io/hostname
            operator: NotIn
            values:
            - arpit-server

redis:
  replica:
    replicaCount: 1
    affinity:
      *affinityNotMaster
  master:
    affinity:
      *affinityNotMaster
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true

cassandra:
  dbUser:
    user: cassandra
    password: vergon
  nodeSelector:
    kubernetes.io/hostname: arpit-server
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
      namespace: ""

provisionDataStore:
  elasticsearch: true

elasticsearch:
  image:
    tag: 7.17.5
  master:
    masterOnly: false
    replicaCount: 1
    autoscaling:
      minReplicas: 1
      maxReplicas: 2
    affinity:
      *affinityNotMaster
  data:
    replicaCount: 0
  ingest:
    replicaCount: 0
  coordinating:
    replicaCount: 0
  security:
    enabled: false
    elasticPassword: password
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true

